<h1>Note <%= @note.id %></h1>
<h3>By: <%= @note.user.name if !@note.user.nil? %></h3>
<p><%= @note.content %></p>


<%= link_to "Edit Note", edit_note_path(@note) %> |

<%= link_to "Delete Note", note_path(@note), method: :delete %> | <%= link_to "New Note", new_note_path %><br />
<p><%= link_to "All Notes", notes_path %></p>
