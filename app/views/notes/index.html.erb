<% if session[:name] %>
  <h2>Hi, <%= session[:name] %>!</h2>
  <%= @notes.size %><br></br>
<% else %>
  <%= form_tag('/login', method: :post, class: 'session-login') do %>
    <%= label_tag :name %>
    <%= text_field_tag :name %>
    <br>
    <%= submit_tag "login" %>
  <% end %>
<% end %>
