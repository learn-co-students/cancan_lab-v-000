<%= form_for @note do |f| %>
  <%= f.text_area :content, class: "form-control" %>
  <%= f.hidden_field :user_id, value: session[:user_id] %>
  <%= f.text_area :visible_to, value: @note.visible_to, class: "form-control" %>
  <%= f.submit %>
<% end %>
