<div> New Note </div>

<%= form_for @note do |f| %>
	<%= f.label :user %>
	<%= f.text_field :user %>

	<%= f.label :content %>
	<%= f.text_field :content %>
  	
  	<%= f.label :visible_to %>
  	<%= f.text_field :visible_to %>

  	<%= f.submit "Submit" %>
  	
  
<% end %> 

