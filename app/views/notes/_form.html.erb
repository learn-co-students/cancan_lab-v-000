<%= form_for(note) do |f| %> 
    <p><%= f.label :content %></p>
    <p><%= f.text_area :content %></p>
    <p><%= f.label :visible_to %></p>
    <p><%= f.text_field :visible_to %></p>
    <p><%= f.submit %></p>
<% end %>