<%= form_for @note do |f| %>
  <%= f.label :content %> <br>
  <%= f.text_area :content %> <br>
  <%= f.label "Who this will be visible to, comma separated"%> <br>
  <%= f.text_field :visible_to %><br>
  <%= f.submit %>
<% end %>
